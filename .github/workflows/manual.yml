on:
    workflow_dispatch:
        inputs:
            environment:
                description: "Select environment"
                required: true
                type: choice
                options:
                  - dev
                  - staging
                  - production
        
jobs:
  run-manually:
    runs-on: ubuntu-latest
    steps:
        - name: run manually
          run: echo "App runs on ${{ github.event.inputs.environment }}"

